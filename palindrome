#include <stdio.h>

int main() {
  int isPalindrome(int *arr, int size);
  
  int n;
  
   printf("Enter array size: ");
    scanf("%d", &n);

    int a [n];
    printf("Enter elements: ");
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
	}

	int b = 0, r = n - 1, p = 1;
	while (b < r){
		if (a[b] != a[r] ){
			p = 0;
			break;
			
		} 
		b++;
		r--;
		
	}
	
    if (p) {
        printf("PALINDROME\n");
    } else {
        printf("NOT PALINDROME\n");
    }
	
	return 0;
	
}
